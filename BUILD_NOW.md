=== FunnelSwift-Mobile: BUILD REQUEST ===

The .env file is ready at /opt/swift/FunnelSwift-Mobile/.env. EAS is fully configured.

Trigger the build:
  cd /opt/swift/FunnelSwift-Mobile && npx eas build --platform android --profile preview

This produces an APK for internal distribution. Report the build URL/ID back to David.

Note: First build prompts for credentials — choose auto-generate.
